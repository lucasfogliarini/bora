# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - divagando

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: azure/docker-login@v1
      with:
        login-server: divagando.azurecr.io
        username: ${{ secrets.acr_divagando_username }}
        password: ${{ secrets.acr_divagando_password }}
        
    - name: Build and push image to ACR
      id: build-image
      run: |
          docker build . -f  Dockerfile -t divagando.azurecr.io/divagando:${{ github.sha }} --label dockerfile-path=Dockerfile
          docker push divagando.azurecr.io/divagando:${{ github.sha }}

  deploy:
    if: true
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'divagando'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_6a4b33619bde4b7bba54770d1ed5cc81 }}
        images: 'divagando.azurecr.io/divagando:${{ github.sha }}'
